- name: The following is installed {{ item }}
  apt:
      name: "{{ item }}"
      state: present
  with_items:
                  - git
                  - python3
                  - gcc
                  - g++ 
                  - cmake
                  - make
                  - libxml2-dev
                  - libboost-dev
                  - libusb-dev
                  - libpcre3-dev
                  - re2c 
                  - libx11-dev
                  - libxext-dev
                  - libopencv-dev
                  - libzbar-dev
                  - libreadline-dev
                  - libusb-1.0-0-dev
                  - libdmtx-dev